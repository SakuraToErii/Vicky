interface:
  display_name: "Vicky Ask"
  short_description: "Query the maintained wiki with seed-and-neighbor citations."
  brand_color: "#2E7D6B"
  default_prompt: "Use $ask to answer a Vicky wiki question through retrieve.py, then cite wiki pages with [[slug]] links."

policy:
  allow_implicit_invocation: true
